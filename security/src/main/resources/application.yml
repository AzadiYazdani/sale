server:
  port: 8081
#  servlet:
#    contextPath: /api/v3

spring:
  application:
    name: haraji
  profiles:
    active: dev
  jpa:
    hibernate:
      ddl-auto: update

  datasource:
    url: jdbc:mysql://localhost:3306/sale_db
    username: sale_user
    password: S@Idaza1
    driver-class-name: com.mysql.cj.jdbc.Driver

security:
  type: DB
  oauth2:
    token-uri: jdbc:mysql://localhost:3306/sale_db

jwt:
  timeout: 10
  secret-key: asdfSFS34wfsdfsdfSDSD32dfsddDDerQSNCK34SOWEK5354fdgdf4
  refresh-threshold-ms: 3600000  # 1 ساعت - زمانی که مانده به انقضا برای تمدید
  expiration-ms: 86400000  # 24 ساعت
  token-string:
    header: Authorization
    prefix: Bearer

#Swagger: API documentation
swagger:
  api:
    title: General haraji services @${spring.profiles.active}
    description: General haraji REST Services API Documentation
    version: 1
    api-docs-path: /v2/api-docs
  enabled: true

